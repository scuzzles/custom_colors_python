# custom_colors_python
A super simple python module that allows you to set the color of terminal text using hex codes
# Docs
I'm lazy but I'll do it eventually. Basically `use_hex("#hex-code")` and `esc` should cover it, make a class
